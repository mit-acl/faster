# Params for REACT planner
thresh: 0.3       # Distance used to cluster scan (m)
debug: true
safe_distance: 2  # Smallest distance before obstacle is no longer viable segment
buffer: 0.42       # 0.5 width of quad for collision checking

# (x,y,z) goal
goal_x: 0
goal_y: -6.0
goal_z: 0.75

# Flight params
speed: 3

jerk: 30  
accel: 10

# Trajectory evaluation rate
plan_eval: 0.01 

# Number of samples from k-d tree to find obstacle 
K: 50

# Number of samples within buffer to be deemed collision
K_buff: 20

# Sensor horizontal FOV (deg)
h_fov: 60
